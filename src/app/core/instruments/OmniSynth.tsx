import styles from "../../../config/styles";

import type { MonoSynthOptions } from "tone";

export default function OmniSynth(options: MonoSynthOptions) {
  return (
    <section>
      <h1 className={styles.headings.h1}>OmniSynth</h1>
      <p>This synth is soon available.</p>
    </section>
  );
}
